---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: virtualclustertemplates.storage.loft.sh
spec:
  group: storage.loft.sh
  names:
    kind: VirtualClusterTemplate
    listKind: VirtualClusterTemplateList
    plural: virtualclustertemplates
    singular: virtualclustertemplate
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: VirtualClusterTemplate holds the virtualClusterTemplate information
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VirtualClusterTemplateSpec holds the specification
            properties:
              access:
                description: Access holds the access rights for users and teams
                items:
                  description: Access describes the access to a secret
                  properties:
                    name:
                      description: Name is an optional name that is used for this
                        access rule
                      type: string
                    subresources:
                      description: Subresources defines the sub resources that are
                        allowed by this access rule
                      items:
                        type: string
                      type: array
                    teams:
                      description: Teams specifies which teams should be able to access
                        this secret with the aforementioned verbs
                      items:
                        type: string
                      type: array
                    users:
                      description: Users specifies which users should be able to access
                        this secret with the aforementioned verbs
                      items:
                        type: string
                      type: array
                    verbs:
                      description: Verbs is a list of Verbs that apply to ALL the
                        ResourceKinds and AttributeRestrictions contained in this
                        rule. VerbAll represents all kinds.
                      items:
                        type: string
                      type: array
                  required:
                  - verbs
                  type: object
                type: array
              description:
                description: Description describes the virtual cluster template
                type: string
              displayName:
                description: DisplayName is the name that is shown in the UI
                type: string
              owner:
                description: Owner holds the owner of this object
                properties:
                  team:
                    description: Team specifies a Loft team.
                    type: string
                  user:
                    description: User specifies a Loft user.
                    type: string
                type: object
              parameters:
                description: Parameters define additional app parameters that will
                  set helm values
                items:
                  properties:
                    defaultValue:
                      description: DefaultValue is the default value if none is specified
                      type: string
                    description:
                      description: Description is the description to show for this
                        parameter
                      type: string
                    invalidation:
                      description: Invalidation regex that if matched will reject
                        the input
                      type: string
                    label:
                      description: Label is the label to show for this parameter
                      type: string
                    max:
                      description: Max is the maximum number if type is number
                      type: integer
                    min:
                      description: Min is the minimum number if type is number
                      type: integer
                    options:
                      description: Options are the options if type is enum
                      items:
                        type: string
                      type: array
                    placeholder:
                      description: Placeholder shown in the UI
                      type: string
                    required:
                      description: Required specifies if this parameter is required
                      type: boolean
                    section:
                      description: Section where this app should be displayed. Apps
                        with the same section name will be grouped together
                      type: string
                    type:
                      description: 'Type of the parameter. Can be one of: string,
                        multiline, boolean, enum and password'
                      type: string
                    validation:
                      description: Validation regex that if matched will allow the
                        input
                      type: string
                    variable:
                      description: Variable is the path of the variable. Can be foo
                        or foo.bar for nested objects.
                      type: string
                  type: object
                type: array
              spaceTemplateRef:
                description: 'DEPRECATED: SpaceTemplate to use to create the virtual
                  cluster space if it does not exist'
                properties:
                  name:
                    description: Name of the space template
                    type: string
                type: object
              template:
                description: Template holds the virtual cluster template
                properties:
                  access:
                    description: Access defines the access of users and teams to the
                      virtual cluster.
                    properties:
                      defaultClusterRole:
                        description: Specifies which cluster role should get applied
                          to users or teams that do not match a rule below.
                        type: string
                      rules:
                        description: Rules defines which users and teams should have
                          which access to the virtual cluster. If no rule matches
                          an authenticated incoming user, the user will get cluster
                          admin access.
                        items:
                          properties:
                            clusterRole:
                              description: ClusterRole is the cluster role that should
                                be assigned to the
                              type: string
                            teams:
                              description: Teams that this rule matches.
                              items:
                                type: string
                              type: array
                            users:
                              description: Users this rule matches. * means all users.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                  accessPoint:
                    description: AccessPoint defines settings to expose the virtual
                      cluster directly via an ingress rather than through the (default)
                      Loft proxy
                    properties:
                      ingress:
                        description: Ingress defines virtual cluster access via ingress
                        properties:
                          enabled:
                            description: Enabled defines if the virtual cluster access
                              point (via ingress) is enabled or not; requires the
                              connected cluster to have the `loft.sh/ingress-suffix`
                              annotation set to define the domain name suffix used
                              for the ingress.
                            type: boolean
                        type: object
                    type: object
                  apps:
                    description: Apps specifies the apps that should get deployed
                      by this template
                    items:
                      properties:
                        name:
                          description: Name of the target app
                          type: string
                        namespace:
                          description: Namespace specifies in which target namespace
                            the app should get deployed in
                          type: string
                        parameters:
                          description: Parameters to use for the app
                          type: string
                        releaseName:
                          description: ReleaseName is the name of the app release
                          type: string
                        version:
                          description: Version of the app
                          type: string
                      type: object
                    type: array
                  charts:
                    description: Charts are helm charts that should get deployed
                    items:
                      properties:
                        insecureSkipTlsVerify:
                          description: If tls certificate checks for the chart download
                            should be skipped
                          type: boolean
                        name:
                          description: Name is the chart name in the repository
                          type: string
                        password:
                          description: The password that is required for this repository
                          type: string
                        releaseName:
                          description: ReleaseName is the preferred release name of
                            the app
                          type: string
                        releaseNamespace:
                          description: ReleaseNamespace is the preferred release namespace
                            of the app
                          type: string
                        repoURL:
                          description: RepoURL is the repo url where the chart can
                            be found
                          type: string
                        timeout:
                          description: Timeout is the time to wait for any individual
                            Kubernetes operation (like Jobs for hooks) (default 5m0s)
                          type: string
                        username:
                          description: The username that is required for this repository
                          type: string
                        values:
                          description: Values are the values that should get passed
                            to the chart
                          type: string
                        version:
                          description: Version is the chart version in the repository
                          type: string
                        wait:
                          description: Wait determines if Loft should wait during
                            deploy for the app to become ready
                          type: boolean
                      type: object
                    type: array
                  helmRelease:
                    description: HelmRelease is the helm release configuration for
                      the virtual cluster.
                    properties:
                      chart:
                        description: infos about what chart to deploy
                        properties:
                          name:
                            description: the name of the helm chart
                            type: string
                          repo:
                            description: the repo of the helm chart
                            type: string
                          version:
                            description: the version of the helm chart to use
                            type: string
                        type: object
                      values:
                        description: the values for the given chart
                        type: string
                    type: object
                  instanceTemplate:
                    description: InstanceTemplate holds the virtual cluster instance
                      template
                    properties:
                      metadata:
                        description: The virtual cluster instance metadata
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations are annotations on the object
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            description: Labels are labels on the object
                            type: object
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                  metadata:
                    description: The virtual cluster metadata
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are annotations on the object
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are labels on the object
                        type: object
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  objects:
                    description: Objects are Kubernetes style yamls that should get
                      deployed into the virtual cluster
                    type: string
                  spaceTemplate:
                    description: SpaceTemplate holds the space template
                    properties:
                      apps:
                        description: Apps specifies the apps that should get deployed
                          by this template
                        items:
                          properties:
                            name:
                              description: Name of the target app
                              type: string
                            namespace:
                              description: Namespace specifies in which target namespace
                                the app should get deployed in
                              type: string
                            parameters:
                              description: Parameters to use for the app
                              type: string
                            releaseName:
                              description: ReleaseName is the name of the app release
                              type: string
                            version:
                              description: Version of the app
                              type: string
                          type: object
                        type: array
                      charts:
                        description: Charts are helm charts that should get deployed
                        items:
                          properties:
                            insecureSkipTlsVerify:
                              description: If tls certificate checks for the chart
                                download should be skipped
                              type: boolean
                            name:
                              description: Name is the chart name in the repository
                              type: string
                            password:
                              description: The password that is required for this
                                repository
                              type: string
                            releaseName:
                              description: ReleaseName is the preferred release name
                                of the app
                              type: string
                            releaseNamespace:
                              description: ReleaseNamespace is the preferred release
                                namespace of the app
                              type: string
                            repoURL:
                              description: RepoURL is the repo url where the chart
                                can be found
                              type: string
                            timeout:
                              description: Timeout is the time to wait for any individual
                                Kubernetes operation (like Jobs for hooks) (default
                                5m0s)
                              type: string
                            username:
                              description: The username that is required for this
                                repository
                              type: string
                            values:
                              description: Values are the values that should get passed
                                to the chart
                              type: string
                            version:
                              description: Version is the chart version in the repository
                              type: string
                            wait:
                              description: Wait determines if Loft should wait during
                                deploy for the app to become ready
                              type: boolean
                          type: object
                        type: array
                      metadata:
                        description: The space metadata
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations are annotations on the object
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            description: Labels are labels on the object
                            type: object
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      objects:
                        description: Objects are Kubernetes style yamls that should
                          get deployed into the virtual cluster namespace
                        type: string
                    type: object
                  workloadVirtualClusterTemplate:
                    description: WorkloadVirtualClusterTemplateDefinition holds the
                      workload cluster specific deployment options. Needs to be non-nil
                      in order to deploy the virtual cluster in workload cluster mode.
                    properties:
                      helmRelease:
                        description: HelmRelease is the helm release configuration
                          for the virtual cluster.
                        properties:
                          chart:
                            description: infos about what chart to deploy
                            properties:
                              name:
                                description: the name of the helm chart
                                type: string
                              repo:
                                description: the repo of the helm chart
                                type: string
                              version:
                                description: the version of the helm chart to use
                                type: string
                            type: object
                          values:
                            description: the values for the given chart
                            type: string
                        type: object
                      metadata:
                        description: The virtual cluster metadata
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations are annotations on the object
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            description: Labels are labels on the object
                            type: object
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      spaceTemplate:
                        description: SpaceTemplate holds the space template
                        properties:
                          apps:
                            description: Apps specifies the apps that should get deployed
                              by this template
                            items:
                              properties:
                                name:
                                  description: Name of the target app
                                  type: string
                                namespace:
                                  description: Namespace specifies in which target
                                    namespace the app should get deployed in
                                  type: string
                                parameters:
                                  description: Parameters to use for the app
                                  type: string
                                releaseName:
                                  description: ReleaseName is the name of the app
                                    release
                                  type: string
                                version:
                                  description: Version of the app
                                  type: string
                              type: object
                            type: array
                          charts:
                            description: Charts are helm charts that should get deployed
                            items:
                              properties:
                                insecureSkipTlsVerify:
                                  description: If tls certificate checks for the chart
                                    download should be skipped
                                  type: boolean
                                name:
                                  description: Name is the chart name in the repository
                                  type: string
                                password:
                                  description: The password that is required for this
                                    repository
                                  type: string
                                releaseName:
                                  description: ReleaseName is the preferred release
                                    name of the app
                                  type: string
                                releaseNamespace:
                                  description: ReleaseNamespace is the preferred release
                                    namespace of the app
                                  type: string
                                repoURL:
                                  description: RepoURL is the repo url where the chart
                                    can be found
                                  type: string
                                timeout:
                                  description: Timeout is the time to wait for any
                                    individual Kubernetes operation (like Jobs for
                                    hooks) (default 5m0s)
                                  type: string
                                username:
                                  description: The username that is required for this
                                    repository
                                  type: string
                                values:
                                  description: Values are the values that should get
                                    passed to the chart
                                  type: string
                                version:
                                  description: Version is the chart version in the
                                    repository
                                  type: string
                                wait:
                                  description: Wait determines if Loft should wait
                                    during deploy for the app to become ready
                                  type: boolean
                              type: object
                            type: array
                          metadata:
                            description: The space metadata
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations are annotations on the object
                                type: object
                              labels:
                                additionalProperties:
                                  type: string
                                description: Labels are labels on the object
                                type: object
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          objects:
                            description: Objects are Kubernetes style yamls that should
                              get deployed into the virtual cluster namespace
                            type: string
                        type: object
                    type: object
                type: object
              versions:
                description: Versions are different versions of the template that
                  can be referenced as well
                items:
                  properties:
                    parameters:
                      description: Parameters define additional app parameters that
                        will set helm values
                      items:
                        properties:
                          defaultValue:
                            description: DefaultValue is the default value if none
                              is specified
                            type: string
                          description:
                            description: Description is the description to show for
                              this parameter
                            type: string
                          invalidation:
                            description: Invalidation regex that if matched will reject
                              the input
                            type: string
                          label:
                            description: Label is the label to show for this parameter
                            type: string
                          max:
                            description: Max is the maximum number if type is number
                            type: integer
                          min:
                            description: Min is the minimum number if type is number
                            type: integer
                          options:
                            description: Options are the options if type is enum
                            items:
                              type: string
                            type: array
                          placeholder:
                            description: Placeholder shown in the UI
                            type: string
                          required:
                            description: Required specifies if this parameter is required
                            type: boolean
                          section:
                            description: Section where this app should be displayed.
                              Apps with the same section name will be grouped together
                            type: string
                          type:
                            description: 'Type of the parameter. Can be one of: string,
                              multiline, boolean, enum and password'
                            type: string
                          validation:
                            description: Validation regex that if matched will allow
                              the input
                            type: string
                          variable:
                            description: Variable is the path of the variable. Can
                              be foo or foo.bar for nested objects.
                            type: string
                        type: object
                      type: array
                    template:
                      description: Template holds the space template
                      properties:
                        access:
                          description: Access defines the access of users and teams
                            to the virtual cluster.
                          properties:
                            defaultClusterRole:
                              description: Specifies which cluster role should get
                                applied to users or teams that do not match a rule
                                below.
                              type: string
                            rules:
                              description: Rules defines which users and teams should
                                have which access to the virtual cluster. If no rule
                                matches an authenticated incoming user, the user will
                                get cluster admin access.
                              items:
                                properties:
                                  clusterRole:
                                    description: ClusterRole is the cluster role that
                                      should be assigned to the
                                    type: string
                                  teams:
                                    description: Teams that this rule matches.
                                    items:
                                      type: string
                                    type: array
                                  users:
                                    description: Users this rule matches. * means
                                      all users.
                                    items:
                                      type: string
                                    type: array
                                type: object
                              type: array
                          type: object
                        accessPoint:
                          description: AccessPoint defines settings to expose the
                            virtual cluster directly via an ingress rather than through
                            the (default) Loft proxy
                          properties:
                            ingress:
                              description: Ingress defines virtual cluster access
                                via ingress
                              properties:
                                enabled:
                                  description: Enabled defines if the virtual cluster
                                    access point (via ingress) is enabled or not;
                                    requires the connected cluster to have the `loft.sh/ingress-suffix`
                                    annotation set to define the domain name suffix
                                    used for the ingress.
                                  type: boolean
                              type: object
                          type: object
                        apps:
                          description: Apps specifies the apps that should get deployed
                            by this template
                          items:
                            properties:
                              name:
                                description: Name of the target app
                                type: string
                              namespace:
                                description: Namespace specifies in which target namespace
                                  the app should get deployed in
                                type: string
                              parameters:
                                description: Parameters to use for the app
                                type: string
                              releaseName:
                                description: ReleaseName is the name of the app release
                                type: string
                              version:
                                description: Version of the app
                                type: string
                            type: object
                          type: array
                        charts:
                          description: Charts are helm charts that should get deployed
                          items:
                            properties:
                              insecureSkipTlsVerify:
                                description: If tls certificate checks for the chart
                                  download should be skipped
                                type: boolean
                              name:
                                description: Name is the chart name in the repository
                                type: string
                              password:
                                description: The password that is required for this
                                  repository
                                type: string
                              releaseName:
                                description: ReleaseName is the preferred release
                                  name of the app
                                type: string
                              releaseNamespace:
                                description: ReleaseNamespace is the preferred release
                                  namespace of the app
                                type: string
                              repoURL:
                                description: RepoURL is the repo url where the chart
                                  can be found
                                type: string
                              timeout:
                                description: Timeout is the time to wait for any individual
                                  Kubernetes operation (like Jobs for hooks) (default
                                  5m0s)
                                type: string
                              username:
                                description: The username that is required for this
                                  repository
                                type: string
                              values:
                                description: Values are the values that should get
                                  passed to the chart
                                type: string
                              version:
                                description: Version is the chart version in the repository
                                type: string
                              wait:
                                description: Wait determines if Loft should wait during
                                  deploy for the app to become ready
                                type: boolean
                            type: object
                          type: array
                        helmRelease:
                          description: HelmRelease is the helm release configuration
                            for the virtual cluster.
                          properties:
                            chart:
                              description: infos about what chart to deploy
                              properties:
                                name:
                                  description: the name of the helm chart
                                  type: string
                                repo:
                                  description: the repo of the helm chart
                                  type: string
                                version:
                                  description: the version of the helm chart to use
                                  type: string
                              type: object
                            values:
                              description: the values for the given chart
                              type: string
                          type: object
                        instanceTemplate:
                          description: InstanceTemplate holds the virtual cluster
                            instance template
                          properties:
                            metadata:
                              description: The virtual cluster instance metadata
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations are annotations on the
                                    object
                                  type: object
                                labels:
                                  additionalProperties:
                                    type: string
                                  description: Labels are labels on the object
                                  type: object
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                          type: object
                        metadata:
                          description: The virtual cluster metadata
                          properties:
                            annotations:
                              additionalProperties:
                                type: string
                              description: Annotations are annotations on the object
                              type: object
                            labels:
                              additionalProperties:
                                type: string
                              description: Labels are labels on the object
                              type: object
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        objects:
                          description: Objects are Kubernetes style yamls that should
                            get deployed into the virtual cluster
                          type: string
                        spaceTemplate:
                          description: SpaceTemplate holds the space template
                          properties:
                            apps:
                              description: Apps specifies the apps that should get
                                deployed by this template
                              items:
                                properties:
                                  name:
                                    description: Name of the target app
                                    type: string
                                  namespace:
                                    description: Namespace specifies in which target
                                      namespace the app should get deployed in
                                    type: string
                                  parameters:
                                    description: Parameters to use for the app
                                    type: string
                                  releaseName:
                                    description: ReleaseName is the name of the app
                                      release
                                    type: string
                                  version:
                                    description: Version of the app
                                    type: string
                                type: object
                              type: array
                            charts:
                              description: Charts are helm charts that should get
                                deployed
                              items:
                                properties:
                                  insecureSkipTlsVerify:
                                    description: If tls certificate checks for the
                                      chart download should be skipped
                                    type: boolean
                                  name:
                                    description: Name is the chart name in the repository
                                    type: string
                                  password:
                                    description: The password that is required for
                                      this repository
                                    type: string
                                  releaseName:
                                    description: ReleaseName is the preferred release
                                      name of the app
                                    type: string
                                  releaseNamespace:
                                    description: ReleaseNamespace is the preferred
                                      release namespace of the app
                                    type: string
                                  repoURL:
                                    description: RepoURL is the repo url where the
                                      chart can be found
                                    type: string
                                  timeout:
                                    description: Timeout is the time to wait for any
                                      individual Kubernetes operation (like Jobs for
                                      hooks) (default 5m0s)
                                    type: string
                                  username:
                                    description: The username that is required for
                                      this repository
                                    type: string
                                  values:
                                    description: Values are the values that should
                                      get passed to the chart
                                    type: string
                                  version:
                                    description: Version is the chart version in the
                                      repository
                                    type: string
                                  wait:
                                    description: Wait determines if Loft should wait
                                      during deploy for the app to become ready
                                    type: boolean
                                type: object
                              type: array
                            metadata:
                              description: The space metadata
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations are annotations on the
                                    object
                                  type: object
                                labels:
                                  additionalProperties:
                                    type: string
                                  description: Labels are labels on the object
                                  type: object
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            objects:
                              description: Objects are Kubernetes style yamls that
                                should get deployed into the virtual cluster namespace
                              type: string
                          type: object
                        workloadVirtualClusterTemplate:
                          description: WorkloadVirtualClusterTemplateDefinition holds
                            the workload cluster specific deployment options. Needs
                            to be non-nil in order to deploy the virtual cluster in
                            workload cluster mode.
                          properties:
                            helmRelease:
                              description: HelmRelease is the helm release configuration
                                for the virtual cluster.
                              properties:
                                chart:
                                  description: infos about what chart to deploy
                                  properties:
                                    name:
                                      description: the name of the helm chart
                                      type: string
                                    repo:
                                      description: the repo of the helm chart
                                      type: string
                                    version:
                                      description: the version of the helm chart to
                                        use
                                      type: string
                                  type: object
                                values:
                                  description: the values for the given chart
                                  type: string
                              type: object
                            metadata:
                              description: The virtual cluster metadata
                              properties:
                                annotations:
                                  additionalProperties:
                                    type: string
                                  description: Annotations are annotations on the
                                    object
                                  type: object
                                labels:
                                  additionalProperties:
                                    type: string
                                  description: Labels are labels on the object
                                  type: object
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                            spaceTemplate:
                              description: SpaceTemplate holds the space template
                              properties:
                                apps:
                                  description: Apps specifies the apps that should
                                    get deployed by this template
                                  items:
                                    properties:
                                      name:
                                        description: Name of the target app
                                        type: string
                                      namespace:
                                        description: Namespace specifies in which
                                          target namespace the app should get deployed
                                          in
                                        type: string
                                      parameters:
                                        description: Parameters to use for the app
                                        type: string
                                      releaseName:
                                        description: ReleaseName is the name of the
                                          app release
                                        type: string
                                      version:
                                        description: Version of the app
                                        type: string
                                    type: object
                                  type: array
                                charts:
                                  description: Charts are helm charts that should
                                    get deployed
                                  items:
                                    properties:
                                      insecureSkipTlsVerify:
                                        description: If tls certificate checks for
                                          the chart download should be skipped
                                        type: boolean
                                      name:
                                        description: Name is the chart name in the
                                          repository
                                        type: string
                                      password:
                                        description: The password that is required
                                          for this repository
                                        type: string
                                      releaseName:
                                        description: ReleaseName is the preferred
                                          release name of the app
                                        type: string
                                      releaseNamespace:
                                        description: ReleaseNamespace is the preferred
                                          release namespace of the app
                                        type: string
                                      repoURL:
                                        description: RepoURL is the repo url where
                                          the chart can be found
                                        type: string
                                      timeout:
                                        description: Timeout is the time to wait for
                                          any individual Kubernetes operation (like
                                          Jobs for hooks) (default 5m0s)
                                        type: string
                                      username:
                                        description: The username that is required
                                          for this repository
                                        type: string
                                      values:
                                        description: Values are the values that should
                                          get passed to the chart
                                        type: string
                                      version:
                                        description: Version is the chart version
                                          in the repository
                                        type: string
                                      wait:
                                        description: Wait determines if Loft should
                                          wait during deploy for the app to become
                                          ready
                                        type: boolean
                                    type: object
                                  type: array
                                metadata:
                                  description: The space metadata
                                  properties:
                                    annotations:
                                      additionalProperties:
                                        type: string
                                      description: Annotations are annotations on
                                        the object
                                      type: object
                                    labels:
                                      additionalProperties:
                                        type: string
                                      description: Labels are labels on the object
                                      type: object
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                objects:
                                  description: Objects are Kubernetes style yamls
                                    that should get deployed into the virtual cluster
                                    namespace
                                  type: string
                              type: object
                          type: object
                      type: object
                    version:
                      description: Version is the version. Needs to be in X.X.X format.
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: VirtualClusterTemplateStatus holds the status
            type: object
        type: object
    served: true
    storage: true
